A CHIP-8 Emulator.

Written in Ruby, because there's probably already a thousand C versions of this
  emulator, and Ruby is more fun anyway.

This project is two firsts for me, namely:
  - Doing test / behavior driven development. 
  - Creating an emulator (much harder, I think?).
